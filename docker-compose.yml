version: '3'

services:

    php:
        container_name: websockets-workshop-1-php
        build: ./docker
        ports:
            - 1337:80
            - 1338:1338


        volumes:
            - ./:/appdata